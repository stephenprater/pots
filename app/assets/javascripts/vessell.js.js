// Generated by CoffeeScript 1.3.1

$(function() {
  var vessell_search;
  vessell_search = Search["new"]('#vessells', Routes.vessells_path());
  vessell_search.reset();
  $('#vessell_accession_number').bind('railsAutocomplete.select', function(obj, data) {
    vessell_search.add_params({
      product_search: $(this.val())
    });
    $.ajax(vessell_search.to_s(), {
      dataType: 'script'
    });
    return $(this).val('');
  }).keypress(function(event) {
    if (event.which === 13) {
      vessell_search.add_params({
        product_search: $(this).val()
      });
      event.preventDefault();
      return $.ajax(vessell_search.to_s(), {
        dataType: 'script'
      });
    }
  });
  return true;
});
